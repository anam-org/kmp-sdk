package ai.anam.lab.client.core.client.di

actual interface ClientPlatformContextSubgraph {
    // Since the PlatformContext is a typealias for Android's Context, this is already exposed to the dependency graph.
}
